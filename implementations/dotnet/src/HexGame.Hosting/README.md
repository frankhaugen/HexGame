# HexGame.Hosting

Provides `IFixedTickSource`, `FixedTickSource`, `IGameHost`, and `LocalGameHost` for running a generic `IGameApplication` with a fixed simulation tick rate in-process.

Depends on [HexGame.Abstractions](../HexGame.Abstractions/README.md) only (no generic host or DI container required for v0.1).
