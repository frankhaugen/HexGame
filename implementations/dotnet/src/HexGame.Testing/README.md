# HexGame.Testing

Test doubles (`InMemoryCommandInbox`, `CapturingFramePublisher`, `HexGameScenario`) for exercising `IGameApplication` implementations deterministically. Framework-agnostic (no xUnit dependency in this package).

Depends on [HexGame.Abstractions](../HexGame.Abstractions/README.md) only.
